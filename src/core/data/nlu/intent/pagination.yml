version: "3.1"

nlu:
  - regex: limit
    examples: |
      - [placeholder] localized in en/us or locale overlays

  - regex: offset
    examples: |
      - [placeholder] localized in en/us or locale overlays
