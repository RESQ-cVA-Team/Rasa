action_endpoint:
  url: ${ACTION_ENDPOINT_URL}
  timeout: 300
lock_store:
  type: redis
  url: ${LOCK_STORE_URL}
  db: ${LOCK_STORE_DB}
tracker_store:
  type: redis
  url: ${TRACKER_STORE_URL}
  db: ${TRACKER_STORE_DB}
cors:
  enabled: false
  allowed_origins: []